// Agent 08 implements this.
import { Keypair } from '@stellar/stellar-sdk'
export type { Keypair }
export function createStellarSigner(secretKey: string): Keypair {
  throw new Error('Not implemented — Agent 08')
}
